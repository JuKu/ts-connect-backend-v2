import {Module} from "@nestjs/common";

@Module({})
/**
 * This module is responsible for all the app / backend statistics.
 * These statistics are shown to administrators.
 *
 * @author Justin Kuenzel
 */
export class StatisticsModule {}
